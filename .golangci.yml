linters:
  enable-all: true
  disable:
    - gomnd
    - gochecknoglobals
    - lll
    - funlen
    - wsl
    - gocognit
    - scopelint
    - gochecknoinits
    - unparam
    - interfacer
    - prealloc

issues:
  exclude-rules:
    - path: _test.go
      linters:
        - goconst
