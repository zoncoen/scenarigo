schemaVersion: config/v1

scenarios:
- scenarios

pluginDirectory: ./plugin/gen
plugins:
  plugin.so:
    src: ./plugin/src

input:
  excludes:
  - \.ytt\.yaml$
  yaml:
    ytt:
      enabled: true
      defaultFiles:
      - default.yaml
