linters:
  enable-all: true
  disable:
    - dupl
    - funlen
    - gci
    - gochecknoglobals
    - gochecknoinits
    - gocognit
    - godox
    - goerr113
    - gomnd
    - ifshort
    - ireturn
    - lll
    - nestif
    - nlreturn
    - prealloc
    - scopelint
    - testpackage
    - unparam
    - wsl

    # deplicated
    - exhaustivestruct
    - golint
    - interfacer
    - maligned
    - scopelint

  skip-dirs:
    - gen

issues:
  exclude-rules:
    - path: _test.go
      linters:
        - goconst
        - noctx
    - linters:
        - stylecheck
      text: "ST1016:"

linters-settings:
  exhaustive:
    default-signifies-exhaustive: true
